# A bunch of keywords. No errors should be present.
int real bool string true false void
if else elif
for while do break continue
+ - * / % = [ ] ( ) { } 
> >= < <= ! != == ,

+= -= /= *= # Assignment operators

|| &&       # Logical operators 

--, ++      # Unary operators
